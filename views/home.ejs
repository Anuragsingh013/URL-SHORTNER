<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Getting data from server</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
    }

    body {
      height: 100vh;
      width: 100vw;
      display: flex;
      justify-content: center;
      align-items: center;
      background-image: linear-gradient(180deg, #1d20cc, #1d66cc);
      backdrop-filter: blur(40px);
    }

    .container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1.4rem;
      flex-direction: column;
      background: #fff;
      color: #000;
      flex-wrap: wrap;
      min-height: 40vh;
      min-width: 30vw;
      border-radius: 24px;
    }

    .container h1 {
      margin: 12px 0;
    }

    .container p {
      font-size: 1.4rem;
      color: #000;
    }

    .container label {
      font-size: 1.4rem;
      /* Allow text wrapping */
      word-wrap: break-word;
      white-space: normal;
    }

    button {
      background: #1d66cc;
      color: #fff;
    }

    button:hover {
      background: #1d20cc;
      color: #fff;
    }

    .container input,
    button {
      padding: 12px 24px;
      font-size: 16px;
      outline: 1px solid #1d20cc;
      border: 0;
      text-align: center;
      border-radius: 40px;
    }

    .generatorBox {
      padding-top: 16px;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      flex-direction: column;
      flex-wrap: wrap;
    }

    .generatorBox input {
      margin: 16px 0;
      border-radius: 40px;
      width: 100%;
    }

    p span {
      color: blue;
      cursor: pointer;
      width: 100%;
    }

    @media screen and (max-width: 730px) {
      .container {
        padding: 1rem;
        min-width: 80vw;
        border-radius: 16px;
      }

      .container h1 {
        margin: 12px 0;
        font-size: 1.2rem;
      }

      .container p {
        font-size: 1.2rem;
        width: 90%;
        padding: 1rem;
      }

      .container label {
        font-size: 1.2rem;
      }

      .container input,
      button {
        padding: 10px 20px;
        font-size: 14px;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>URL Shortener</h1>
    <% if(locals.id) { %>
      <p>URL generated: <span>http://localhost:8001/<%=id%></span>
      </p>
      <% } %>
        <form action="/url" method="POST">
          <div class="generatorBox">
            <label>Enter your original URL</label>
            <input type="text" name="url" placeholder="Enter here...">
            <button type="submit">Generate</button>
          </div>
        </form>
  </div>

</body>




</html>